project: lhc-olympics-2020
seed: 42

data:
  raw_dir: data/raw
  processed_dir: data/processed

model:
  type: autoencoder
  input_dim: 2100
  latent_dim: 16

train:
  batch_size: 512
  lr: 0.001
  epochs: 20
  device: cuda
  num_workers: 0
  use_amp: true

outputs:
  root: outputs
  models: outputs/models
  logs: outputs/logs
  figures: outputs/figures
